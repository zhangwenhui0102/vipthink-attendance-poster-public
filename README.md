# VIPTHINK Attendance Poster Public Host

This repository hosts the current public poster image used for DingTalk image delivery.

Stable image URL target:

- `teacher_attendance_teacher_group_review.jpg`

The file may be updated over time while keeping the same path so downstream robot configuration can stay unchanged.
